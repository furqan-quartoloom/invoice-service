services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/invoices.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
